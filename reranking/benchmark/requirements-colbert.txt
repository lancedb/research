# PyLate pins sentence-transformers 5.3, so ColBERT models run in a separate environment.
lancedb @ git+https://github.com/lancedb/lancedb.git@ayush/cross-encoder-kwargs#subdirectory=python
pylate==1.6.0
datasets
